<svg width="1200" height="630" viewBox="0 0 1200 630" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#2d3f54"/>

  <defs>
    <linearGradient id="gammaGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#8B5CF6"/>
      <stop offset="100%" stop-color="#3B82F6"/>
    </linearGradient>
  </defs>

  <!-- Robot Logo (centered, scaled 2.5x) -->
  <g transform="translate(437, 30) scale(2.5)">
    <!-- Antenna -->
    <rect x="61" y="0" width="8" height="25" rx="4" fill="url(#gammaGrad)"/>
    <circle cx="65" cy="0" r="10" fill="#22D3EE"/>

    <!-- Bot head -->
    <rect x="0" y="30" width="130" height="110" rx="30" fill="url(#gammaGrad)"/>

    <!-- Left eye -->
    <circle cx="37" cy="75" r="18" fill="white"/>
    <circle cx="41" cy="77" r="10" fill="#1E293B"/>
    <circle cx="44" cy="74" r="4" fill="white"/>

    <!-- Right eye -->
    <circle cx="93" cy="75" r="18" fill="white"/>
    <circle cx="97" cy="77" r="10" fill="#1E293B"/>
    <circle cx="100" cy="74" r="4" fill="white"/>

    <!-- Gamma mouth -->
    <text
      x="65"
      y="125"
      font-family="Georgia, serif"
      font-size="40"
      font-style="italic"
      fill="white"
      text-anchor="middle"
      opacity="0.95"
    >γ</text>
  </g>

  <!-- GAMMABOTS text -->
  <text
    x="600"
    y="480"
    font-family="Inter, system-ui, sans-serif"
    font-size="90"
    font-weight="600"
    fill="white"
    text-anchor="middle"
    letter-spacing="0.05em"
  >GAMMABOTS</text>

  <!-- Tagline -->
  <text
    x="600"
    y="560"
    font-family="Inter, system-ui, sans-serif"
    font-size="40"
    font-weight="400"
    fill="#94a3b8"
    text-anchor="middle"
    letter-spacing="0.025em"
  >Trading bots. Simplified.</text>
</svg>
